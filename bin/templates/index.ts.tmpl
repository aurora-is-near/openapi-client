import { getRequestFunction } from '../create';
import type { OpenApiClientOptions } from '../options';

{{#each services}}
import type { components as {{name}}Types } from './{{dir}}/types';
import {
  getOperations as get{{name}}Operations,
  type {{name}}Methods,
  type {{name}}Client,
  type {{name}}Request,
} from './{{dir}}/client';
{{/each}}

{{#each services}}
export type {{name}}Models = {{name}}Types['schemas'];

export {
  type {{name}}Methods,
  type {{name}}Client,
  type {{name}}Request,
  get{{name}}Operations,
};
{{/each}}
{{#each services}}

export const create{{name}}Client = (
  options: OpenApiClientOptions,
): {{name}}Client => {
  const request = getRequestFunction('{{title}}', '{{version}}', options);

  return get{{name}}Operations(request);
};
{{/each}}
